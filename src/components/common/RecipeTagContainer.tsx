import { styled } from '@mui/material/styles';
import { Box } from './Box';

export const RecipeTagContainer = styled(Box)(() => ({
  display: 'flex',
  flexWrap: 'wrap',
  gap: 6,
  marginTop: 4,
}));



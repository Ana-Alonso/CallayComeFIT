import { styled } from '@mui/material/styles';
import { Box } from './Box';

export const RecipeCardTop = styled(Box)(() => ({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
}));



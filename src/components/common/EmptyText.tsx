import { styled } from '@mui/material/styles';
import { Box } from './Box';

export const EmptyText = styled(Box)(() => ({
  fontSize: 13,
  color: '#a0a0ab',
  maxWidth: 320,
  lineHeight: 1.5,
}));



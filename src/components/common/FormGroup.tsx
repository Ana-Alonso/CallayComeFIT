import { styled } from '@mui/material/styles';
import { Box } from './Box';

export const FormGroup = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  gap: 6,
  marginBottom: 16,
}));



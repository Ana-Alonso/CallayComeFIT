import { styled } from '@mui/material/styles';
import { Box } from './Box';

export const FilterSection = styled(Box)(() => ({
  marginBottom: 20,
  display: 'flex',
  flexDirection: 'column',
  gap: 8,
}));



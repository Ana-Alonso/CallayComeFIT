import { styled } from '@mui/material/styles';
import { Box } from './Box';

export const DayCardHeader = styled(Box)(() => ({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  marginBottom: 12,
}));
